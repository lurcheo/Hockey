namespace Hockey.Client.Shared.View;

public static class GlobalRegions
{
    private const string Key = "Global";
    public const string Main = Key + nameof(Main);
    public const string Settings = Key + nameof(Settings);

    public const string GuestTeam = Key + nameof(GuestTeam);
    public const string HomeTeam = Key + nameof(HomeTeam);
}
